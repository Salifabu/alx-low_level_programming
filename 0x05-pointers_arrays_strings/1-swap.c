#include "main.h"

/**
 * Swap_int -swaps the values of two integers
 * @a: integer to swap
 * @b: An integer input pointer
 * Return: Always 0
 */

void swap_int(int *a, int *b);

{
int c;
c = *a;
*a = *b;
*b = c;
}
